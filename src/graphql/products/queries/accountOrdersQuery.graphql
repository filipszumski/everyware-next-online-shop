query GetAccountOrders($email: String!) {
  account(where: { email: $email }) {
    orders {
      id
      total
      orderItems {
        quantity
        total
        product {
          id
          name
          images {
            url
          }
        }
      }
    }
  }
}
